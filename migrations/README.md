# Migrations

**These are reference copies only.** The authoritative migrations live in the iOS repo:

- `00010_create_posts.sql` — posts table
- `00011_create_client_profiles.sql` — client profiles table + test seed

Both tables are already migrated and the test client is seeded (UUID: `00000000-0000-0000-0000-000000000001`).

Do NOT run the SQL files in this folder — they exist only for documentation.
